# Terraform Plan Report (Basic)

**Generated:** 2025-12-07 23:08:51  
**Plan File:** ..\samples\google_plan_complex_1.txt

---

## Summary

| Metric | Count |
|--------|-------|
| Resources to Add | 3 |
| Resources to Change | 2 |
| Resources to Destroy | 1 |
| **Total Changes** | 321 |

---

## Resource Changes

### 1. ➕ CREATED - module.project.module.network.google_compute_firewall.ssh

**Action:** created

**Resource Name:** `module.project.module.network.google_compute_firewall.ssh`

**Changes:**
```
+ resource "google_compute_firewall" "ssh" {
+ name    = "allow-ssh-from-bastion"
+ network = module.project.module.network.google_compute_network.main.name
+ project = "sample-project"
+ allow {
+ protocol = "tcp"
+ ports    = ["22"]
+ source_ranges = [
+ target_tags = ["bastion"]
```

### 2. 🔄 UPDATED IN-PLACE - module.project.module.network.google_compute_subnetwork.private[0]

**Action:** updated in-place

**Resource Name:** `module.project.module.network.google_compute_subnetwork.private[0]`

**Changes:**
```
~ resource "google_compute_subnetwork" "private" {
~ secondary_ip_range = [
+ {
+ {
~ networking_mode          = "VPC_NATIVE" -> "VPC_NATIVE"
~ enable_autopilot         = false -> true # forces replacement
~ remove_default_node_pool = true -> true
- master_authorized_networks_config {
- cidr_blocks {
- cidr_block   = "0.0.0.0/0"
- display_name = "public-access"
+ release_channel {
+ channel = "REGULAR"
```

### 3. ➕ CREATED - module.project.module.gke.google_container_node_pool.system

**Action:** created

**Resource Name:** `module.project.module.gke.google_container_node_pool.system`

**Changes:**
```
+ resource "google_container_node_pool" "system" {
+ name       = "system-pool"
+ cluster    = google_container_cluster.primary.name
+ location   = "us-central1"
+ node_count = 3
+ node_config {
+ machine_type = "e2-standard-4"
+ disk_size_gb = 100
+ oauth_scopes = ["https://www.googleapis.com/auth/cloud-platform"]
+ labels = {
+ management {
+ auto_repair  = true
+ auto_upgrade = true
```

### 4. 🔄 UPDATED IN-PLACE - module.project.module.sql.google_sql_database_instance.primary

**Action:** updated in-place

**Resource Name:** `module.project.module.sql.google_sql_database_instance.primary`

**Changes:**
```
~ resource "google_sql_database_instance" "primary" {
~ settings {
~ tier = "db-custom-2-7680" -> "db-custom-4-15360"
~ backup_configuration {
~ enabled = false -> true
+ insights_config {
+ query_insights_enabled  = true
+ query_string_length     = 1024
+ record_application_tags = true
```

### 5. ➕ CREATED - module.project.module.sql.google_sql_user.readonly

**Action:** created

**Resource Name:** `module.project.module.sql.google_sql_user.readonly`

**Changes:**
```
+ resource "google_sql_user" "readonly" {
+ instance = google_sql_database_instance.primary.name
+ name     = "readonly_user"
+ password = (sensitive value)
+ type     = "BUILT_IN"
```

### 6. ❌ DESTROYED - module.project.google_pubsub_topic.audit

**Action:** destroyed

**Resource Name:** `module.project.google_pubsub_topic.audit`

**Changes:**
```
- resource "google_pubsub_topic" "audit" {
- name  = "legacy-audit-topic" -> null
- labels = {
- "system" = "legacy"
```

---

## Notes

This is a **basic report** generated by parsing Terraform plan output.

- ✅ Extracted resource names and actions
- ✅ Listed attribute changes
- ❌ No impact analysis
- ❌ No risk assessment
- ❌ No recommendations
- ❌ No dependency analysis

**For detailed analysis with AI:**
- Risk assessment
- Impact analysis
- Deployment recommendations
- Rollback procedures
- Testing checklists

Use the AI-enhanced version of this tool.

---

**Report Type:** Basic (No AI)  
**Parser Version:** 1.0  
**Generation Time:** 2025-12-07 23:08:51

---

<div align="center">

*Generated using:* [**Terraform AI Assistance**](https://marketplace.visualstudio.com/items?itemName=cloudcraft.terraform-report-generator)

</div>
